#include<iostream>

class SubSequence{
// we need to create a function to find a few subsequences of the string 
public: 
     void sequences(String str){
   // this function finds all the subsequences combination 
     // 
     }
      void subsequence(String str){
        if(str.length()==1)return ; 

      }
};

int main(){
  //  
}